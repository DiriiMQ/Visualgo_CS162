temp = new Node(value)
if list.head == null and position != 0:
    return "Error: List is empty"
if position == 0:
    temp.next = list.head
    list.head = temp
else:
    current = list.head
    for i in range(1, position):
        current = current.next
    temp.next = current.next
    current.next = temp